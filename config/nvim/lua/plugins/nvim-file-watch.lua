-- Used to reload files after Claude Code changes them
return {
  -- "awalland/nvim-file-watch",
  dir = "~/.config/nvim/lua/plugin-sources/nvim-file-watch",
  opts = {
    flash_hl = "DiffChange",
  },
}
